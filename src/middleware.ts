export { default } from "next-auth/middleware";

export const config = {
	// matcher: ["/", "/articulo/:path*", "/api/:path*"],
	// matcher: ["/api/:path*"],
	matcher: ["/home"],
};
// "/distrinova/:path*"
//
